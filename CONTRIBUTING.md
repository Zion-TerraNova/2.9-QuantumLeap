# Contributing

## Ground rules
- Do not commit secrets (keys, mnemonics, private endpoints).
- Keep all example hosts generic (`example.com`, `127.0.0.1`).

## Development
- Use Python 3.10+
- Run formatting/linting before PRs (see `miner/` README)
